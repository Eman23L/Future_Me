# FutureMe Open Questions

- Should each user have auth/accounts or stay device-based for now?
- How should reminders map to users before auth?
- Should Supabase cron call Vercel endpoint or Edge Function?
- How should schedule edits sync with scheduled reminders?
- Should missed tasks roll forward?
- Should missed tasks stay active until completed?
- How should weekly check-in replan the week?
- How much should What’s Next automatically change vs suggest?
- Should fixed commitments ever be moved?
- How should FutureMe handle user overwhelm?
- How should the app behave if notifications are disabled?
