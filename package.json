{
  "name": "future-me",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "compile": "tsc -p tsconfig.vite.json",
    "dev": "npm run build && node scripts/serve-dist.cjs",
    "build": "npm run compile && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "postinstall": "node scripts/patch-vite-windows-realpath.cjs"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.9.0",
    "vite": "^5.4.20",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0"
  }
}
